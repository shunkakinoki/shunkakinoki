// Copyright 2019 Shun Kakinoki.

#include <algorithm>
#include <cmath>
#include <cstdio>
#include <iostream>
#include <vector>
using namespace std;

int main() {
  // WIP
  // FAILED THIS PROBLEM
  int n, q;
  cin >> n;
  cin >> q;

  for (int i = 0; i < n; i++) {
    cin >> k;
    int a[i][k];
    for (int j = 0; j < k; j++) {
      cin >> e;
      a[i][j] == e;
    }
  }

  for (int i = 0; i < q; i++) {
    int a, b;
    cin >> a >> b;
    cout << a[i][b] << endl;
  }

  return 0;
}
